Today is 30 March 2026. In this website we have created so many animations, like the hello glow on Friday or the whole sidebar with the screen drag and drop system. The main goal of these animations and UI is to recreate them in the zeded code editor using Rust and GPUI.Do research about zed, Rust, GPUI, and create a summary explaining all the animations and how they are done in this website so that we can recreate these animations in the GPU IRAS zed code editor. First of all study the whole codebase and search for all the animations so that I can clarify what animations we should have a summary of because there are many animations and we don't need to recreate them all in the zed code editor.

In the summary please mention all the animation details, functionality, and logic of the whole sidebar, the screen swiper, the hello glow, the friday animation, and all the stuff.

I told you we don't have to mention all the animations remove the: eyes, (Glass Crack, Spider-Man) and hello glow, friday border, sidebar, screen carosel are very impotant and need to be implemented - and mention that we will copy the website code next to the GPUI source code so that we can learn from it and implement it correctly. Also we will create a new tab code editor, like the onboarding untitled tab. In that tab in the GPUI code editor we will put and show all the dimensions. As we will do all of this animation directly in the get code editor fork

Now also mention that we have to implement our current website sidebar UI and all of its functionality and logic into the zedcode editor's workspace sidebar that is in the actual zedcode editor. We have to update that to be like our sidebar. We also have to make the zedcode editor tabs be our screen cursor to have the functionality of our screen cursor. At the top of the zedcode editor we will create a macOS-like dock that can control the screen cursor so that we can control the zedcode editor tabs from the top with a macOS-like dock. 

1. liquid Glass
2. Animation
3. Svg
4. Theme, icon, font
5. rainbow animated cursor, particles effect

Today is 30 March 2026 and I am forking zedcode editor. In that zedcode editor I will render video, audio, image, and 3D. I will also put liquid glass animations, macOS Apple-like soft smooth animations, and busy animations on the icons. Tell me how can I do it by doing web stars and deep teachers.

I already created the liquid glass component for GPUI. Now I only need the smooth animations so please tell me how to implement the Mac OS-like and Apple iPhone-like smooth animation on our Zed code editor. I have to also put the animated SVGs so please tell me how to implement that. For video, audio, and image, they are only rendered by the Zed code editor so we don't have to worry about that; we just have to worry about video rendering and 3D rendering. Please give me the best Rust crates to make the video player and 3D player in our Zed code editor.
