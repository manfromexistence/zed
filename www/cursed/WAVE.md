Like the gunshot please add this spiderman waves as the special effect in our welcome screen!!!

``tsx "use client";  import { AnimatePresence, motion } from "motion/react"; import { memo, useCallback, useEffect, useRef, useState } from "react";  // ─── Constants ───────────────────────────────────────────────────────────────  const WAVE_LIFETIME_MS = 3_500; const ANIMATION_DURATION_MS = 2_000; const CANVAS_FPS = 15; // Spider-Verse style choppy frame rate const CANVAS_FRAME_MS = 1000 / CANVAS_FPS;  const NUM_RINGS = 8; const NUM_SPEED_LINES = 16; const NUM_DOT_RINGS = 6; const DOTS_PER_RING = 20; const NUM_GLITCH_BARS = 5; const NUM_WEB_STRANDS = 8; const TAU = Math.PI * 2;  // Spider-Verse palette const RED = { r: 220, g: 38, b: 38 } as const; const BLUE = { r: 37, g: 99, b: 235 } as const; const PINK = { r: 236, g: 72, b: 153 } as const; const CYAN = { r: 6, g: 182, b: 212 } as const;  // ─── Types ───────────────────────────────────────────────────────────────────  interface Point {   readonly x: number;   readonly y: number; }  interface RGB {   readonly r: number;   readonly g: number;   readonly b: number; }  interface GlitchBar {   readonly yPercent: number;   readonly height: number;   readonly offset: number;   readonly delay: number;   readonly colorIndex: number; }  interface WaveInstance {   readonly id: number;   readonly x: number;   readonly y: number;   readonly glitchBars: readonly GlitchBar[]; }  interface SpidermanWavesModeProps {   readonly active: boolean;   readonly onTrigger?: () => void; }  // ─── Easing ──────────────────────────────────────────────────────────────────  function easeOutCubic(t: number): number {   return 1 - (1 - t) ** 3; }  function easeOutQuart(t: number): number {   return 1 - (1 - t) ** 4; }  // ─── Pre-computed Flicker Table (deterministic per-frame noise) ──────────────  const FLICKER_SIZE = 64; const FLICKER = Float32Array.from(   { length: FLICKER_SIZE },   () => 0.4 + Math.random() * 0.6, );  // ─── Helpers ─────────────────────────────────────────────────────────────────  function rgba(c: RGB, a: number): string {   return `rgba(${c.r},${c.g},${c.b},${a})`; }  // ─── Canvas Renderer ─────────────────────────────────────────────────────────  /**  * Draws a single animation frame of the Spider-Verse wave effect:  *   1. Concentric expanding rings (red / blue alternating with glow)  *   2. Halftone Ben-Day dots (pink / cyan)  *   3. Radial speed lines with deterministic flicker  *   4. Central impact burst (white → pink radial gradient)  */ function drawWaveFrame(   ctx: CanvasRenderingContext2D,   center: Point,   progress: number,   w: number,   h: number,   dpr: number,   frame: number, ): void {   ctx.clearRect(0, 0, w * dpr, h * dpr);   ctx.save();   ctx.setTransform(dpr, 0, 0, dpr, 0, 0);    const maxR = Math.hypot(w, h) * 0.6;    // ── 1. Concentric rings ──────────────────────────────────────────────────    for (let i = 0; i < NUM_RINGS; i++) {     const stagger = i * 0.06;     const t = Math.max(0, Math.min(1, (progress - stagger) / (1 - stagger * 0.5)));     if (t <= 0) continue;      const eased = easeOutCubic(t);     const fade = 1 - eased;     const alpha = fade * 0.7;     if (alpha < 0.01) continue;      const color = i & 1 ? BLUE : RED;      ctx.globalAlpha = alpha;     ctx.strokeStyle = rgba(color, 1);     ctx.lineWidth = Math.max(1, (NUM_RINGS - i) * 3 * fade);     ctx.shadowBlur = 15 * fade;     ctx.shadowColor = rgba(color, alpha * 0.6);      ctx.beginPath();     ctx.arc(center.x, center.y, eased * maxR, 0, TAU);     ctx.stroke();   }    ctx.shadowBlur = 0;    // ── 2. Halftone dots (batched per ring) ──────────────────────────────────    const dotStep = TAU / DOTS_PER_RING;    for (let ring = 0; ring < NUM_DOT_RINGS; ring++) {     const t = Math.max(       0,       Math.min(1, (progress - ring * 0.08 - 0.1) / 0.5),     );     if (t <= 0) continue;      const ringR = (ring + 1) * (maxR / (NUM_DOT_RINGS + 2)) * easeOutCubic(t);     const alpha = (1 - t) * 0.5;     if (alpha < 0.01) continue;      const size = Math.max(1, (5 - ring * 0.4) * (1 - t * 0.5));     const color = ring & 1 ? CYAN : PINK;      ctx.globalAlpha = alpha;     ctx.fillStyle = rgba(color, 1);      // Batch all dots in this ring into one path     ctx.beginPath();     for (let d = 0; d < DOTS_PER_RING; d++) {       const a = dotStep * d + ring * 0.2;       const dx = center.x + Math.cos(a) * ringR;       const dy = center.y + Math.sin(a) * ringR;       ctx.moveTo(dx + size, dy);       ctx.arc(dx, dy, size, 0, TAU);     }     ctx.fill();   }    // ── 3. Radial speed lines ────────────────────────────────────────────────    const lt = Math.max(0, Math.min(1, (progress - 0.05) / 0.6));   if (lt > 0) {     const le = easeOutQuart(lt);     const la = (1 - lt) * 0.5;      if (la > 0.01) {       const step = TAU / NUM_SPEED_LINES;        for (let i = 0; i < NUM_SPEED_LINES; i++) {         const a = step * i;         const cos = Math.cos(a);         const sin = Math.sin(a);         const inner = 30 + le * 20;         const outer = inner + le * maxR * 0.7;         const flicker = FLICKER[(frame * NUM_SPEED_LINES + i) & (FLICKER_SIZE - 1)];          ctx.globalAlpha = la * flicker;         ctx.strokeStyle = rgba(i & 1 ? BLUE : RED, 1);         ctx.lineWidth = Math.max(0.5, 2.5 * (1 - le));          ctx.beginPath();         ctx.moveTo(center.x + cos * inner, center.y + sin * inner);         ctx.lineTo(center.x + cos * outer, center.y + sin * outer);         ctx.stroke();       }     }   }    // ── 4. Central impact burst ──────────────────────────────────────────────    if (progress < 0.4) {     const bt = progress / 0.4;     const be = easeOutCubic(bt);     const br = be * 60;     const ba = (1 - bt) * 0.9;      const g = ctx.createRadialGradient(center.x, center.y, 0, center.x, center.y, br);     g.addColorStop(0, `rgba(255,255,255,${ba})`);     g.addColorStop(0.3, rgba(PINK, ba * 0.7));     g.addColorStop(1, rgba(RED, 0));      ctx.globalAlpha = 1;     ctx.fillStyle = g;     ctx.beginPath();     ctx.arc(center.x, center.y, br, 0, TAU);     ctx.fill();   }    ctx.restore(); }  // ─── Data Generation ─────────────────────────────────────────────────────────  function generateGlitchBars(): GlitchBar[] {   return Array.from({ length: NUM_GLITCH_BARS }, (_, i) => ({     yPercent: Math.random() * 100,     height: 2 + Math.random() * 8,     offset: (Math.random() - 0.5) * 30,     delay: Math.random() * 0.2,     colorIndex: i & 1,   })); }  // ─── Hoisted Style Objects ───────────────────────────────────────────────────  const CANVAS_STYLE: React.CSSProperties = { mixBlendMode: "screen" }; const CURSOR_STYLE: React.CSSProperties = { willChange: "transform" }; const FLASH_STYLE: React.CSSProperties = {   background: "white",   mixBlendMode: "overlay", }; const GLITCH_COLORS = [rgba(RED, 0.3), rgba(BLUE, 0.3)] as const; const GLITCH_BG: React.CSSProperties = { mixBlendMode: "screen" };  // ─── SpiderCrosshair ─────────────────────────────────────────────────────────  const STRAND_TRANSFORMS = Array.from(   { length: NUM_WEB_STRANDS },   (_, i) => `translate(0,-50%) rotate(${(360 / NUM_WEB_STRANDS) * i}deg)`, );  const SpiderCrosshair = memo(function SpiderCrosshair() {   return (     <>       {/* Central pulse */}       <div className="absolute left-1/2 top-1/2 h-2.5 w-2.5 -translate-x-1/2 -translate-y-1/2 rounded-full bg-red-500 shadow-[0_0_12px_rgba(220,38,38,0.8)]">         <div className="absolute inset-0 animate-ping rounded-full bg-red-400 opacity-60" />       </div>        {/* Web strands */}       {STRAND_TRANSFORMS.map((transform, i) => (         <div           key={i}           className="absolute left-1/2 top-1/2 h-px w-10 origin-left bg-gradient-to-r from-red-500/80 to-transparent"           style={{ transform }}         />       ))}        {/* Inner ring */}       <div className="absolute left-1/2 top-1/2 h-8 w-8 -translate-x-1/2 -translate-y-1/2 rounded-full border border-red-500/40" />        {/* Outer ring */}       <div className="absolute left-1/2 top-1/2 h-16 w-16 -translate-x-1/2 -translate-y-1/2 rounded-full border border-blue-500/30" />     </>   ); });  // ─── WaveEffect ──────────────────────────────────────────────────────────────  interface WaveEffectProps {   readonly x: number;   readonly y: number;   readonly glitchBars: readonly GlitchBar[]; }  const WaveEffect = memo(function WaveEffect({ x, y, glitchBars }: WaveEffectProps) {   const canvasRef = useRef<HTMLCanvasElement>(null);    useEffect(() => {     const canvas = canvasRef.current;     if (!canvas) return;      const ctx = canvas.getContext("2d");     if (!ctx) return;      const dpr = window.devicePixelRatio || 1;     const w = window.innerWidth;     const h = window.innerHeight;     canvas.width = w * dpr;     canvas.height = h * dpr;      const center: Point = { x, y };     const t0 = performance.now();     let rafId: number;     let lastDraw = 0;     let frame = 0;      const tick = (now: number) => {       const progress = Math.min(1, (now - t0) / ANIMATION_DURATION_MS);        // Throttle draw rate for Spider-Verse choppy aesthetic       if (now - lastDraw >= CANVAS_FRAME_MS) {         lastDraw = now - ((now - lastDraw) % CANVAS_FRAME_MS);         drawWaveFrame(ctx, center, progress, w, h, dpr, frame++);       }        if (progress < 1) {         rafId = requestAnimationFrame(tick);       }     };      rafId = requestAnimationFrame(tick);     return () => cancelAnimationFrame(rafId);   }, [x, y]);    // Pre-build style objects that depend on coordinates   const redGlow: React.CSSProperties = {     background: `radial-gradient(circle at ${x}px ${y}px, ${rgba(RED, 0.2)} 0%, transparent 60%)`,     mixBlendMode: "screen",   };   const blueGlow: React.CSSProperties = {     background: `radial-gradient(circle at ${x}px ${y}px, ${rgba(BLUE, 0.2)} 0%, transparent 60%)`,     mixBlendMode: "screen",   };    return (     <motion.div       initial={{ opacity: 1 }}       exit={{ opacity: 0 }}       transition={{ duration: 0.8 }}       className="pointer-events-none fixed inset-0 z-50"     >       {/* Wave canvas */}       <canvas         ref={canvasRef}         className="absolute inset-0 h-full w-full"         style={CANVAS_STYLE}       />        {/* Chromatic aberration — red channel */}       <motion.div         className="absolute inset-0"         initial={{ opacity: 0 }}         animate={{ opacity: [0, 0.15, 0] }}         transition={{ duration: 0.3, times: [0, 0.3, 1] }}         style={redGlow}       />        {/* Chromatic aberration — blue channel (offset) */}       <motion.div         className="absolute inset-0"         initial={{ opacity: 0, x: 4 }}         animate={{ opacity: [0, 0.12, 0], x: [4, -3, 0] }}         transition={{ duration: 0.3, times: [0, 0.3, 1] }}         style={blueGlow}       />        {/* Glitch displacement bars */}       {glitchBars.map((bar, i) => (         <motion.div           key={i}           className="absolute left-0 right-0"           style={{ top: `${bar.yPercent}%`, height: bar.height }}           initial={{ x: 0, opacity: 0 }}           animate={{             x: [0, bar.offset, -bar.offset * 0.5, 0],             opacity: [0, 0.8, 0.6, 0],           }}           transition={{             duration: 0.4,             delay: bar.delay,             times: [0, 0.2, 0.6, 1],           }}         >           <div             className="h-full w-full"             style={{ ...GLITCH_BG, background: GLITCH_COLORS[bar.colorIndex] }}           />         </motion.div>       ))}        {/* Screen flash */}       <motion.div         className="absolute inset-0"         initial={{ opacity: 0 }}         animate={{ opacity: [0, 0.15, 0] }}         transition={{ duration: 0.15 }}         style={FLASH_STYLE}       />     </motion.div>   ); });  // ─── SpidermanWavesMode (Public API) ─────────────────────────────────────────  export function SpidermanWavesMode({ active, onTrigger }: SpidermanWavesModeProps) {   const [waves, setWaves] = useState<WaveInstance[]>([]);   const crosshairRef = useRef<HTMLDivElement>(null);   const nextId = useRef(0);   const timers = useRef(new Set<number>());    // Crosshair tracks mouse via direct DOM mutation — zero re-renders   useEffect(() => {     if (!active) return;      const onMove = (e: MouseEvent) => {       const el = crosshairRef.current;       if (el) el.style.transform = `translate3d(${e.clientX}px,${e.clientY}px,0)`;     };      window.addEventListener("mousemove", onMove, { passive: true });     return () => window.removeEventListener("mousemove", onMove);   }, [active]);    // Flush state & timers on deactivation   useEffect(() => {     if (!active) {       setWaves([]);       timers.current.forEach(clearTimeout);       timers.current.clear();     }   }, [active]);    // Cleanup on unmount   useEffect(() => {     const t = timers.current;     return () => t.forEach(clearTimeout);   }, []);    const handleClick = useCallback(     (e: React.MouseEvent) => {       if (!active) return;        const id = ++nextId.current;       const { clientX: x, clientY: y } = e;        setWaves((prev) => [         ...prev,         { id, x, y, glitchBars: generateGlitchBars() },       ]);       onTrigger?.();        const timer = window.setTimeout(() => {         setWaves((prev) => prev.filter((w) => w.id !== id));         timers.current.delete(timer);       }, WAVE_LIFETIME_MS);       timers.current.add(timer);     },     [active, onTrigger],   );    if (!active) return null;    return (     <>       <div         ref={crosshairRef}         className="pointer-events-none fixed left-0 top-0 z-[60]"         style={CURSOR_STYLE}       >         <SpiderCrosshair />       </div>        <div className="fixed inset-0 z-[55] cursor-none" onClick={handleClick} />        <AnimatePresence>         {waves.map(({ id, x, y, glitchBars }) => (           <WaveEffect key={id} x={x} y={y} glitchBars={glitchBars} />         ))}       </AnimatePresence>     </>   ); } `` ### Visual breakdown | Layer | What it does | Technique | |---|---|---| | **Concentric rings** | 8 expanding red/blue circles with outer glow that fade as they grow | Canvas arcs with `shadowBlur` | | **Halftone dots** | 6 rings × 20 Ben-Day dots in pink/cyan expanding behind the wave front | Batched canvas arcs (one `fill()` per ring) | | **Speed lines** | 16 radial lines with deterministic per-frame flicker | Canvas lines indexed into `Float32Array` noise table | | **Impact burst** | White→pink radial gradient flash at the click point | Canvas `createRadialGradient` | | **Chromatic aberration** | Red + blue radial overlays with the blue channel offset-animated | `motion.div` with `mixBlendMode: screen` | | **Glitch bars** | 5 horizontal slices that jitter left/right briefly | `motion.div` displacement keyframes | | **Screen flash** | Brief full-screen white pulse | `motion.div` with `mixBlendMode: overlay` | ### Performance highlights - **15 fps canvas throttle** — gives the iconic Spider-Verse choppy look while halving GPU draw calls vs 60 fps - **Batched dot paths** — all 20 dots per halftone ring drawn in a single `ctx.fill()` call - **Pre-computed flicker table** — `Float32Array` lookup replaces `Math.random()` in the hot loop; indexed with bitwise AND for zero-cost wrapping - **Zero-render crosshair** — positioned via direct `style.transform` DOM write with `translate3d` (compositor-only, no layout/paint, no React re-render) - **Hoisted style objects** — `CANVAS_STYLE`, `FLASH_STYLE`, `STRAND_TRANSFORMS`, etc. allocated once at module level - **`memo` on sub-components** — `WaveEffect` and `SpiderCrosshair` skip re-renders when sibling waves are added/removed - **HiDPI-aware canvas** — buffer scaled by `devicePixelRatio`, drawn in logical coordinates via `setTransform` - **Proper cleanup** — all `requestAnimationFrame` and `setTimeout` handles tracked and cancelled on unmount/deactivation
