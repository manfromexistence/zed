IN PROGRESS:
- (none)

PENDING:
- User runs `just run` to validate agent-thread embedded whiteboard/shader web preview on Windows (after dx dev in the examples).

COMPLETED:
- Agent AI screen embedded WWW preview (whiteboard + shader): clicking whiteboard/shader icons in chat input center opens embedded WebPreview in the thread area (replacing message list), with back-to-messages arrow in chat input center; chat input stays pinned at bottom. Implemented via agent_ui hook bridge (avoids agent_ui↔web_preview cycle) + web_preview registration on Windows/macOS/Linux. Source guards updated. rustfmt applied.
- [2026-06-13 fix] Connected correctly to real dx www framework: whiteboard/shader projects are at G:\Dx\www\examples\whiteboard and G:\Dx\www\examples\shader (launched via `dx dev` from those dirs using the `dx` manifest). Fixed root discovery (independent of Zed worktree, ancestor walk + fallback), live port scan from dev logs (to beat port conflicts), removed broken "web/..." + worktree logic. Prevents nav failure before ready. All per DX layout learned from G:\Dx . Direct source edit + rustfmt.

BLOCKED:
- (none)
