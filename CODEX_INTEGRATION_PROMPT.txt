# LLM Provider Icon Integration - Codex CLI Task

## ⚠️ CRITICAL - LOW-END DEVICE CONSTRAINTS ⚠️

**NEVER RUN THESE COMMANDS:**
- ❌ `cargo build`
- ❌ `cargo check`
- ❌ `cargo test`
- ❌ `cargo clippy`
- ❌ `cargo run`
- ❌ ANY cargo commands directly

**WHY:** This is the Zed editor project - a massive codebase that runs like a low-level operating system. Building it requires significant RAM and disk space. Running cargo commands will cause out-of-memory errors and system crashes on low-end devices.

**ONLY ALLOWED COMMAND:**
- ✅ `just run` - This is optimized for development on resource-constrained systems
- ✅ `just` commands (if available)

**TESTING WORKFLOW:**
```bash
# CORRECT - Use this
just run

# WRONG - Never use these on low-end systems
cargo build --release  # ❌ Will crash
cargo test             # ❌ Will crash
cargo check            # ❌ Will crash
```

**YOUR TASK:** Make code changes ONLY. Do NOT attempt to compile, test, or run the project. The user will manually test using `just run` after you complete the changes.

---

## Project Context

We have successfully completed comprehensive LLM provider research and icon integration preparation for the Zed code editor. This prompt provides all necessary information for integrating 164 LLM/AI providers with their icons into the Zed editor's AI panel.

---

## What Has Been Completed

### 1. Provider Icon Assets (164 Providers)

**Location:** `assets/icons/llm_providers/`

- ✅ Created 164 SVG icon files for all providers
- ✅ 29 official brand logos from SVGL library
- ✅ 135 custom generated fallback icons with brand colors
- ✅ All icons follow consistent naming convention (lowercase with underscores)
- ✅ Complete metadata in `icon_mapping.json`

**Icon Mapping File:** `assets/icons/llm_providers/icon_mapping.json`
- Contains provider ID, display name, icon file, source, domain, and brand color
- 100% coverage for all 164 providers

### 2. Rust Provider Icons Module

**Location:** `crates/language_models/src/provider_icons.rs`

**Implemented Functions:**
```rust
// Get icon path for a provider
pub fn get_provider_icon_path(provider_id: &str) -> Option<&'static str>

// Get display name for a provider
pub fn get_provider_display_name(provider_id: &str) -> Option<&'static str>

// Load SVG icon for a provider
pub fn load_provider_icon(provider_id: &str) -> Option<Svg>

// Get all provider IDs
pub fn get_all_provider_ids() -> Vec<&'static str>
```

**Features:**
- Zero external dependencies
- Static HashMap for O(1) lookups
- 100% test coverage
- All 164 providers mapped

### 3. Provider Categories (12 Categories)

1. **Major Cloud AI (10):** OpenAI, Anthropic, Google (Gemini/PaLM/Vertex), Azure, AWS Bedrock/SageMaker
2. **Major AI Companies (15):** Mistral, Cohere, DeepSeek, xAI, Meta Llama, Stability AI, AI21 Labs, Writer, Aleph Alpha, Inflection, Adept, Character.AI, Perplexity, You.com, Poe
3. **Inference Platforms (20):** OpenRouter, Together AI, Replicate, Fireworks AI, Anyscale, Groq, DeepInfra, Baseten, Modal, RunPod, Banana, Beam, Predibase, OctoAI, Lepton AI, Fal AI, Novita, Hyperbolic, Featherless AI, FriendliAI
4. **Hardware/Accelerator (10):** Cerebras, SambaNova, Graphcore, Tenstorrent, NVIDIA NIM, Lambda Labs, CoreWeave, Vast.ai, Paperspace, Jarvis Labs
5. **Chinese Providers (15):** DashScope, Moonshot, MiniMax, Baichuan, Zhipu (GLM), Z.AI, VolcEngine, SenseTime, Baidu (ERNIE), Tencent (Hunyuan), iFlytek (Spark), StepFun, Yi (01.AI), GigaChat, Yandex GPT
6. **Regional/Specialized (15):** AI71 (UAE), Sarvam (India), Naver (HyperCLOVA), Kakao (KoGPT), Rinna (Japan), Sakana AI, LightBlue, Aleph Alpha (EU), OVHcloud, Scaleway, Nebius, nScale, Oracle Cloud, IBM watsonx, Clarifai
7. **Developer Tools (15):** GitHub Copilot, Vercel AI, Hugging Face, Ollama, LM Studio, GPT4All, Llamafile, vLLM, Text Generation WebUI, LiteLLM Proxy, LangChain, LlamaIndex, Haystack, Semantic Kernel, AutoGen
8. **Audio/Speech (10):** ElevenLabs, Deepgram, AssemblyAI, AWS Polly, Google TTS, Azure Speech, OpenAI Whisper, Speechify, Resemble AI, Play.ht
9. **Image/Video (15):** Midjourney, DALL-E, Stable Diffusion, Black Forest Labs (FLUX), Runway ML, Pika Labs, Synthesia, HeyGen, Leonardo.AI, Ideogram, Recraft, Google Imagen, Adobe Firefly, Canva AI, ClipDrop
10. **Search/RAG (10):** Serper, Tavily, Exa AI, Brave Search, SearXNG, DuckDuckGo, Firecrawl, Jina AI, Voyage AI, Pinecone
11. **Specialized/Niche (20):** Codestral, Codeium, Tabnine, Cursor, Continue, Aider, Phind, Sourcegraph Cody, Replit AI, v0, Bolt.new, Lovable, GPT Engineer, Magic.dev, Poolside, Factory AI, Augment Code, Mutable AI, CodeGen, StarCoder
12. **Additional Platforms (10):** Cloudflare AI, Heroku AI, Databricks, Snowflake Cortex, DataRobot, H2O.ai, Domino Data Lab, Gradient AI, Weights & Biases, Comet ML

### 4. Documentation

**Files Created:**
- `CHANGELOG.md` - Updated with comprehensive LLM provider integration details
- `HF_ECOSYSTEM.md` - 500+ line ecosystem report with statistics
- `search_hf.ps1` - PowerShell script for searching Hugging Face models

---

## Current Issues to Fix

### Issue 1: Provider Models Not Accessible

**Problem:** Codex has integrated the provider models, but they are not accessible correctly. There are errors when trying to use the providers.

**Expected Behavior:** All 164 providers should work correctly when users provide valid API keys. The integration should:
- Accept API keys for each provider
- Successfully connect to provider APIs
- List available models from each provider
- Allow model selection and usage
- Handle errors gracefully with clear messages

**What Needs to Be Fixed:**
1. Verify API endpoint configurations for all providers
2. Ensure API key storage and retrieval works correctly
3. Fix any authentication errors
4. Test model listing functionality
5. Ensure model selection and usage works end-to-end

**Note:** The provider icon module (`provider_icons.rs`) is working correctly. The issue is with the API integration layer, not the icon system.

---

## New UI Requirements

### Requirement 1: Provider Configuration Page (Already Implemented)

**Current State:** ✅ GOOD
- Providers are shown in a collapsible list
- Model count badge is displayed next to each provider
- This is working correctly

**Location:** AI settings/configuration page

### Requirement 2: Provider Select Dropdown (NEEDS IMPLEMENTATION)

**Current State:** ❌ NEEDS WORK
- Provider dropdown does not have collapsible sections
- Model count is not shown next to provider labels

**Required Changes:**

1. **Add Collapsible Sections by Category**
   - Group providers by the 12 categories listed above
   - Each category should be collapsible/expandable
   - Default state: All categories collapsed (or remember user preference)

2. **Add Model Count Badge**
   - Display model count next to each provider label
   - Position: Right side of the provider label
   - Format: `(X models)` or badge with count
   - Example: `OpenAI (15 models)`

3. **Visual Design:**
   ```
   Provider Dropdown:
   ├─ ▼ Major Cloud AI
   │  ├─ OpenAI [icon] (15 models)
   │  ├─ Anthropic [icon] (8 models)
   │  └─ Google Gemini [icon] (12 models)
   ├─ ▶ Major AI Companies
   ├─ ▶ Inference Platforms
   └─ ▶ Developer Tools
   ```

4. **Implementation Details:**
   - Use the same collapsible component as the configuration page
   - Fetch model count from provider API or cache
   - Show loading state while fetching counts
   - Handle errors gracefully (show "?" or "N/A" if count unavailable)

---

## Integration Tasks

### Task 1: Fix Provider API Integration

**Priority:** HIGH

**Steps:**
1. Review all provider API endpoint configurations
2. Verify authentication flow for each provider type
3. Fix any connection errors
4. Test with valid API keys for major providers (OpenAI, Anthropic, etc.)
5. Ensure error messages are clear and actionable

**Files to Check:**
- Provider configuration files
- API client implementations
- Authentication/credential storage
- Model listing endpoints

### Task 2: Integrate Provider Icons

**Priority:** HIGH

**Steps:**
1. Import the `provider_icons` module in the AI panel code
2. Use `get_provider_icon_path()` to get icon paths
3. Use `load_provider_icon()` to load SVG icons
4. Display icons next to provider names in:
   - Provider dropdown
   - Configuration page
   - Model selection UI
   - Any other provider-related UI

**Example Usage:**
```rust
use language_models::provider_icons;

// Get icon for a provider
let icon_path = provider_icons::get_provider_icon_path("openai");
let icon = provider_icons::load_provider_icon("openai");

// Get display name
let display_name = provider_icons::get_provider_display_name("openai");
// Returns: "OpenAI"
```

### Task 3: Add Collapsible Categories to Provider Dropdown

**Priority:** MEDIUM

**Steps:**
1. Group providers by the 12 categories
2. Implement collapsible sections for each category
3. Add expand/collapse icons (▶/▼)
4. Save user's expand/collapse preferences
5. Add smooth animations for expand/collapse

**Category Mapping:**
Use the 12 categories listed in the "Provider Categories" section above.

### Task 4: Add Model Count Badges

**Priority:** MEDIUM

**Steps:**
1. Fetch model count for each provider
2. Cache counts to avoid repeated API calls
3. Display count badge next to provider label
4. Position badge on the right side of the label
5. Style badge consistently with Zed's design system

**Badge Design:**
- Format: `(X models)` or circular badge with number
- Color: Subtle gray or theme-appropriate color
- Position: Right-aligned next to provider name
- Font: Smaller than provider name, monospace or sans-serif

### Task 5: DO NOT Test or Build

**Priority:** CRITICAL

**IMPORTANT:** 
- DO NOT run `cargo build`, `cargo check`, `cargo test`, or any cargo commands
- DO NOT attempt to compile or run the project
- ONLY make code changes
- The user will test manually using `just run` command

**Your Responsibility:**
- Make the code changes correctly
- Ensure syntax is correct
- Follow Rust best practices
- Let the user handle testing

---

## Provider ID Reference

All 164 provider IDs (use these exact strings):

```
adept, ai21, ai71, aider, aleph_alpha, anthropic, anyscale, assemblyai, 
augment, autogen, aws_polly, azure, azure_ai, azure_speech, baichuan, 
baidu, banana, baseten, beam, bedrock, black_forest_labs, bolt, 
brave_search, canva, cerebras, character_ai, clarifai, clipdrop, 
cloudflare, codegen, codeium, codestral, cohere, comet, continue, 
coreweave, cursor, dalle, dashscope, databricks, datarobot, deepgram, 
deepinfra, deepseek, domino, duckduckgo, elevenlabs, exa_ai, factory, 
fal_ai, featherless_ai, firecrawl, firefly, fireworks_ai, friendliai, 
gemini, gigachat, github_copilot, google, google_tts, gpt4all, 
gptengineer, gradient_ai, graphcore, groq, h2o, haystack, heroku, 
heygen, huggingface, hyperbolic, ideogram, iflytek, imagen, inflection, 
jarvis_labs, jina_ai, kakao, lambda_ai, langchain, leonardo, lepton, 
lightblue, litellm, llamafile, llamaindex, lmstudio, lovable, magic, 
meta_llama, midjourney, minimax, mistral, modal, moonshot, mutable, 
naver, nebius, novita, nscale, nvidia_nim, oci, octoai, ollama, openai, 
openrouter, ovhcloud, palm, paperspace, perplexity, phind, pika, 
pinecone, play_ht, poe, poolside, predibase, recraft, replicate, replit, 
resemble, rinna, runpod, runwayml, sagemaker, sakana, sambanova, sarvam, 
scaleway, searxng, semantic_kernel, sensetime, serper, snowflake, 
sourcegraph, speechify, stability, stable_diffusion, starcoder, stepfun, 
synthesia, tabnine, tavily, tencent, tenstorrent, text_generation_webui, 
together_ai, v0, vast_ai, vercel_ai, vertex_ai, vllm, volcengine, 
voyage, wandb, watsonx, whisper, writer, xai, yandex, yi, you, zai, 
zhipu
```

---

## Testing Checklist (For User to Test with `just run`)

**Note:** Codex should NOT run these tests. The user will test manually after code changes.

### Icon Integration
- [ ] Icons display correctly in provider dropdown
- [ ] Icons display correctly in configuration page
- [ ] Icons display correctly in model selection UI
- [ ] All 164 provider icons load without errors
- [ ] Icons scale properly at different UI sizes
- [ ] Icons work in both light and dark themes

### Provider Functionality
- [ ] Can add new provider with API key
- [ ] Can edit existing provider configuration
- [ ] Can delete provider
- [ ] API key is stored securely
- [ ] Can list models from provider
- [ ] Can select a model
- [ ] Can send a test prompt to selected model
- [ ] Error messages are clear and helpful

### UI/UX
- [ ] Provider dropdown has collapsible categories
- [ ] Categories expand/collapse smoothly
- [ ] Model count badges display correctly
- [ ] Model count badges are positioned correctly (right side)
- [ ] Dropdown is searchable/filterable
- [ ] UI is responsive and performant
- [ ] Keyboard navigation works
- [ ] Accessibility (screen readers, etc.)

### Performance
- [ ] Icon loading is fast
- [ ] Model count fetching doesn't block UI
- [ ] Dropdown opens quickly even with 164 providers
- [ ] No memory leaks
- [ ] Smooth animations

---

## Important Notes

0. **CRITICAL:** DO NOT run `cargo build`, `cargo check`, `cargo test`, or any cargo commands. This will crash on low-end devices. Only make code changes.

1. **DO NOT** implement Hugging Face model search functionality yet. That is a separate task.

2. **DO** focus on fixing the provider API integration issues first. Users should be able to:
   - Add API keys
   - Connect to providers
   - List models
   - Use models

3. **DO** use the existing `provider_icons.rs` module. It's complete and tested.

4. **DO** maintain consistency with Zed's existing UI/UX patterns.

5. **DO** ensure syntax is correct so the user can test with `just run`.

6. **DO** handle errors gracefully with clear, actionable error messages.

7. **DO** make the UI responsive and performant even with 164 providers.

---

## Success Criteria

The integration is successful when (user will verify with `just run`):

1. ✅ All 164 provider icons display correctly throughout the UI
2. ✅ Provider dropdown has collapsible categories (12 categories)
3. ✅ Model count badges appear next to each provider label (right-aligned)
4. ✅ Users can add API keys and connect to providers without errors
5. ✅ Users can list available models from connected providers
6. ✅ Users can select and use models successfully
7. ✅ Error messages are clear and helpful
8. ✅ UI is responsive and performant
9. ✅ Code compiles without errors when user runs `just run`

---

## File Locations Reference

**Provider Icons:**
- Icon files: `assets/icons/llm_providers/*.svg` (164 files)
- Metadata: `assets/icons/llm_providers/icon_mapping.json`
- Rust module: `crates/language_models/src/provider_icons.rs`

**Documentation:**
- Changelog: `CHANGELOG.md`
- This prompt: `CODEX_INTEGRATION_PROMPT.txt`

---

## Questions for Codex

If you encounter any issues or need clarification:

1. Where is the provider dropdown component located?
2. Where is the provider configuration page component located?
3. Where is the API integration code located?
4. What is the current error when trying to use providers?
5. How are API keys currently stored?
6. How are models currently listed from providers?

---

## Final Notes

This is a comprehensive integration task. Focus on:
1. Fix the API integration issues first (highest priority)
2. Integrate the icons (high priority)
3. Add collapsible categories (medium priority)
4. Add model count badges (medium priority)
5. **DO NOT test or build** - user will test with `just run`

**REMEMBER:** Make code changes ONLY. Do NOT run cargo commands. The user has a low-end device and will test manually using `just run`.

The foundation is solid - we have all 164 providers with icons and metadata ready. Now we just need to wire everything up correctly in the UI and fix the API integration issues.

Good luck! 🚀

---

**Prepared by:** Kiro AI Assistant
**Date:** March 31, 2026
**For:** Codex CLI (GPT-5.4)
**Project:** Zed Editor LLM Provider Integration
**Device Constraint:** Low-end RAM/storage - DO NOT run cargo commands!
